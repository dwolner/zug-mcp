{
  "components": [
    "storage",
    "server"
  ],
  "discoveredDate": "2026-05-24",
  "id": "ISS-029",
  "impact": "reinforcements.jsonl stores pattern text verbatim and matches on equality. An agent observing \"user prefers concise answers\" this session and \"user wants brief responses\" next session creates two separate entries instead of reinforcing one. Without exact text copying, reinforcement counts never accumulate, the tool produces no signal, and zug_get_context surfaces stale or zero-count patterns as if equally weighted.",
  "location": [
    "src/storage.ts",
    "src/server.ts"
  ],
  "phase": null,
  "relatedTickets": [],
  "resolution": "Replaced exact-text matching in reinforcePattern with fuzzy matching: normalized text comparison (case-insensitive, punctuation-stripped) plus Jaccard word-overlap similarity (threshold 0.4, minimum 2 shared content words). The tool now reinforces the closest existing pattern when a near-equivalent is found, rather than creating a duplicate. The tool response also returns the top 5 patterns so agents can see what exists. Updated storage.test.ts to cover fuzzy match, miss, and normalization cases.",
  "resolvedDate": "2026-05-24",
  "severity": "high",
  "status": "resolved",
  "title": "[usability] zug_reinforce_observation requires exact text match — unusable in practice"
}
