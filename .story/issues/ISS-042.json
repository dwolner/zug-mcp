{
  "components": [
    "hooks",
    "cli",
    "setup"
  ],
  "discoveredDate": "2026-05-28",
  "id": "ISS-042",
  "impact": "The PreCompact hook runs `zug compact`, which prints a checkpoint (stats + Active Patterns) to stdout expecting it to be injected as post-compaction context. Per the Claude Code hooks docs (verified 2026-05-28), PreCompact stdout is NOT added to the model's context — only SessionStart stdout is. So the printed checkpoint is dead: the model never sees it. No functional loss today because post-compaction reload is actually handled by the separate SessionStart/`compact` → `zug resume` hook (SessionStart stdout IS injected). Net effect: `zug compact`'s output is redundant/misleading dead code, and the PreCompact hook currently does nothing useful. T-043 repurposes PreCompact for a durability push (`zug push`); this issue tracks cleaning up or removing the ineffective print so the codebase doesn't imply a context-injection that never happens.",
  "location": [
    "src/cli.ts (cmdCompact)",
    "src/setup.ts (mergeClaudeHooks PreCompact registration)"
  ],
  "phase": null,
  "relatedTickets": [
    "T-043"
  ],
  "resolution": "Removed the dead/misleading checkpoint print from cmdCompact in src/cli.ts. The PreCompact hook runs `zug compact`, whose only effective work is the durability push (runPush in synced mode) — PreCompact stdout is NOT injected into the model (only SessionStart stdout is; post-compaction reload is handled by `zug resume` via the SessionStart:compact hook). cmdCompact now performs the durability push and emits a single honest side-effect log line instead of a fake \"# Zug Checkpoint (pre-compact)\" block that implied a context injection that never happens. Updated the `zug compact` usage text accordingly. The setup.ts PreCompact registration was already correct (\"stdout not injected; side effect only\"). Verified: typecheck clean, `zug compact` in local-only prints \"nothing to push\", in synced(unreachable) prints \"durability push → {paused}\"; full suite 176/176 green.",
  "resolvedDate": "2026-06-24",
  "severity": "low",
  "status": "resolved",
  "title": "PreCompact hook checkpoint (zug compact) never reaches the model — stdout not injected"
}
